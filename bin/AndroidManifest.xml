<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.domee"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />
	<uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light.NoTitleBar" >
        <activity
            android:name="com.domee.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DMLoginActivity" android:label="@string/app_name"></activity>
        <activity android:name=".AccListActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMFriendsTimelineActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMAtActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMCommentActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMStatusShowActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMUserTimelineActivity" android:label="@string/app_name"></activity>
        <activity android:name=".DMComposeActivity" android:windowSoftInputMode="adjustResize" android:label="@string/app_name"></activity>
       	<activity android:name=".DMProfileActivity" android:label="@string/app_name"></activity>
       	<activity android:name=".DMAtFriendActivity" android:label="@string/app_name"></activity>
       	<receiver android:name=".broadcast.SendStatusReceiver" android:exported="false">
            <intent-filter >
                <action android:name="SEND_STATUS_ACTION"/>
            </intent-filter>
        </receiver>
    </application>

    
</manifest>